# smart-pot — 프로젝트 배경 문서

이 문서는 코드에서 알 수 없는 맥락(왜 만들었는지, 왜 이렇게 결정했는지)을 정리한 것입니다.
GPT, Claude 등 어떤 AI 도구로 작업하든 이 문서를 먼저 읽고 시작하세요.
"지금 뭐가 됐고 뭐가 남았는지"는 이 문서가 아니라 별도 진행상황 문서를 참고하세요.

## 무엇을 만드는가

연인에게 선물하기 위한 AI 스마트 화분 프로토타입. 식물의 상태(수분, 온습도, 조도 등)를
숫자나 그래프가 아니라 캐릭터 표정으로 보여준다. 표정은 19종 + 전환 애니메이션 9종으로
구성되어 있고, 루나로봇 스타일의 감성적인 캐릭터를 지향한다. 여기에 AI 스피커 기능을 더해
화분과 대화하거나 상태를 물어볼 수 있게 한다.

## 왜 이 프로젝트인가

<!-- TODO(김규민): 아래는 대화에서 파악한 정보만으로 채운 부분입니다. 실제 동기·목표를
     더 구체적으로 적어주시면 좋겠습니다. 예: 왜 "표정"이라는 표현 방식을 선택했는지,
     연인 간에 어떤 경험을 만들고 싶은지, 왜 화분이라는 소재를 골랐는지 등 -->

- 연인 간 선물용 디바이스로, 단순 화분이 아니라 감정적으로 교감할 수 있는 오브젝트를 목표로 함
- 식물 상태를 숫자가 아닌 캐릭터 표정으로 전달해 정서적으로 와닿게 하려는 의도

## 하드웨어/기술 선택과 이유

- **디스플레이**: 두 가지 구현이 공존 중 (아래 "아직 정리 안 된 것" 참고)
  - 시뮬레이터(simulator.py): assets 폴더의 19종 표정 + 9종 전환 PNG 이미지를 프레임으로 순차 출력
  - 실기기용(display.py): 128x64 흑백 OLED(SSD1309, SPI)에 벡터로 직접 그리는 방식,
    표정 6종(happy/thirsty/dark/hot/sleepy/gift)만 구현됨
- **LLM**: Ollama 기반 오픈소스 LLM(현재 config.py 기준 모델명 `gemma4`)을 로컬에서 PC와 연동해서
  사용 중 — 라즈베리파이 자체 성능 한계 때문으로 추정. API 방식(OpenAI 등)으로 바꿀지 재검토 중,
  아직 미결정 (아래 "아직 정리 안 된 것" 참고)
- **STT/TTS**: Whisper base(STT) + Piper(TTS) — 둘 다 로컬 실행 가능한 선택지
- **시뮬레이터**: pygame 기반, 하드웨어 없이도 PC에서 60fps로 표정 전환을 미리 볼 수 있게 구성

## 지금까지의 주요 결정

- **2026-07-07**: 개발 환경을 구 PC(`C:\Users\rlarb\projects\smart-pot`)에서 외장 SSD(`스마트화분/smart-pot`)로 이전
- **2026-07-07**: GitHub 저장소(github.com/rlarbals1210/smart-pot)를 원본(source of truth)으로 삼고,
  SSD는 백업 용도로만 쓰기로 결정. 기존에 별도로 관리하던 `claude-backup` 문서 폴더는 이 결정 이후 삭제
- 표정 시스템을 19종 고정 표정 + 9종 전환 애니메이션으로 설계 (세부 매핑은 아직 확장 중)
- 하드웨어(RPi4, 토양센서, ReSpeaker HAT, DHT22, BH1750, MCP3008 등) 실제 구매 완료

## 아직 정리 안 된 것

- **LLM 방식 재검토 중**: 오픈소스(Ollama, 로컬)를 유지할지 API 방식으로 바꿀지 아직 결론 안 남.
  현재 코드(config.py)는 로컬 Ollama 기준으로 되어 있음
- **디스플레이 구현이 시뮬레이터와 실기기에서 서로 다름**: simulator.py는 19종 표정 + 9종 전환
  PNG 이미지를 쓰는데, display.py(실기기용)는 128x64 흑백 OLED에 벡터로 6종 표정만 그림.
  최종적으로 어떤 디스플레이 하드웨어·표정 세트로 갈지, display.py를 simulator.py 수준으로
  맞출지 결정 필요 (원래 남은 작업 목록의 "display.py 컬러 TFT 이미지 출력 대응"과 연결됨)
- 모바일 앱(연인 간 상태 공유)은 아직 컨셉 단계, 구체적 기획 없음

## 참고

- 저장소: https://github.com/rlarbals1210/smart-pot
- 로컬 작업 위치: 외장 SSD `스마트화분/smart-pot` (Windows: `D:\Window\smart-pot`), 현재 작업 폴더: `~/Developer/smart-pot`
